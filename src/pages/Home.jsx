import React from "react";

function Home() {
  return <section className="home"></section>;
}

export default Home;
/* {data?.data?.map((item) => {
        return (
          <h1 key={item.id}>
            {item.first_name} {item.last_name}
          </h1>
        );
      })} */
